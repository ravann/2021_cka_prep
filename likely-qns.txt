ETCD snapshot and restore
-> the documentation is clear on backup
-> to restore, use the command given without the ip address and add --data-dir="location to restore"

ETCDCTL_API=3 etcdctl snapshot restore snapshotdb --data-dir=""

Custom columns: 
kg pods <pod name> -o json
kg pods -o custom-columns=IMAGE:.spec.containers[*].image,NAME:.spec.containers[*].name,VOL:.spec.volumes[*].name,STATUS:.status.conditions[-1].status,TYPE:.status.conditions[-1].type,CSTATE:.status.containerStatuses[*].state.running.startedAt

Add another scheduler: 
-> Follow the documentation on how to do this, its clean and clear


Exercises: 

Network policy: 
https://kubernetes.io/docs/tasks/administer-cluster/declare-network-policy/

https://github.com/ahmetb/kubernetes-network-policy-recipes


